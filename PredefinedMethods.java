//Calling the predefined methods that is built-in methods which is standard library
public class PredefinedMethods {
    public static void main(String[] args) {
        PredefinedMethods obj = new PredefinedMethods();
        System.out.println(obj.hashCode());
    }
}
